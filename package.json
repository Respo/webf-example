{
  "dependencies": {
    "@calcit/procs": "^0.6.20"
  },
  "scripts": {
    "bundle": "esbuild main.mjs --bundle --outfile=dist/main.js",
    "ui": "webf run dist/main.js"
  },
  "devDependencies": {
    "bottom-tip": "^0.1.3",
    "esbuild": "^0.17.5"
  },
  "version": "0.0.1"
}
